export interface Countries {
	name: string;
}

export interface Cities {
	name: string;
}

export interface Holiday {
	name: string;
	date_year: string;
	date_month: string;
	node_id: string;
	text_html_content?: string;
}
