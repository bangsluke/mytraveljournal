// Let the user toggle on/off a top level dev mode
const DevMode = true;

// Define the image sizes used across the tool
const HolidayCardImageHeight = 150;

const Constants = {
	DevMode,
	HolidayCardImageHeight,
};

export default Constants;
